# CI dependencies for linting and unit tests.
# Airflow remains in requirements.txt for orchestration, but is intentionally
# excluded here because the CI job does not start an Airflow scheduler.
pandas==2.2.3
numpy==2.1.3
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
boto3==1.35.76
moto[s3]==5.0.27
requests==2.32.3
scikit-learn==1.5.2
statsmodels==0.14.4
pytest==8.3.4
ruff==0.8.6

